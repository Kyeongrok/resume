[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "resume"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
build-resume = "build_resume:main"

[tool.setuptools]
py-modules = ["build_resume"]
